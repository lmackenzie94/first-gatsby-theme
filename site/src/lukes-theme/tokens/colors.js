// design tokens
// thanks to component shadowing, creating a directory named after your theme name (i.e. lukes-theme),
// anything we put here will overwrite files of the same name in our theme directory

export default {
  primary: 'hotpink'
};

// if you don't see changes, dump the cache (rm -rf site/.cache/) and restart (yarn workspace site develop) for changes to take effect
